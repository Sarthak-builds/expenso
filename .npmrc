# NOTE: pnpm 11 reads its settings from pnpm-workspace.yaml, NOT this file.
# The real configuration (nodeLinker: hoisted, peer/fetch settings) lives there.
# This file is kept only so non-pnpm tooling has somewhere to look.
# See docs/adr/0009-package-manager-pnpm.md
